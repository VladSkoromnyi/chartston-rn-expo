/**
 * `render/` — Skia drawing (PLAN §5.1). Stage 1 ships the candle geometry builder;
 * grid/axis lines are drawn declaratively in `<Chart/>` for now, and the crosshair
 * lands in Stage 3.
 */

export * from './geometry';
export * from './candles';
export * from './crosshair';
